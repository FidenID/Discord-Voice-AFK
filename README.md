# Discord Voice AFK

Bot Discord super ringan yang join voice channel dan diem aja tanpa play musik.

## Cara Pakai

1. `npm install`
2. Copy `.env.example` ke `.env` dan isi token + ID voice channel
3. `npm start`

Atau pake PM2 biar jalan terus:

```bash
pm2 start index.js --name vc-afk
```
